Para compilar:
$ make

Para executar a biblioteca de eperações de matrizes
$ ./mainEx02 mat_a3x4.example mat_b4x3.example

Para executar a biblioteca de multithread
$ ./main_matrizv4 <matriz A> <matriz B> <numero threads>

obs: não precisa ser essas matrizes, pode criar outras, por o nome que quiser e rodar da mesma forma, mas sempre mantendo a ordem necessária para multiplicação de matrizes, onde o número de colunas da primeira matriz é igual ao número de linhas da segunda matriz. No código a soma está para usar somente a primeira matriz, pois ela sempre tera o mesmo tamanho, necessário na soma. A multiplicação usará as duas matrizes.

Para apagar arquivos
$ make clean